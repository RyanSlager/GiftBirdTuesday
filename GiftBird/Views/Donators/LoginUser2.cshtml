@model GiftBird.Models.LoginModels

@{
	ViewBag.Title = "LoginUser2";
}

<h2>LoginUser2</h2>
@using (Html.BeginForm("Verify", "Donators", FormMethod.Post))
{


        <fieldset class="form-group">
            <div>
                @Html.Label("User Name", new { style = "display:inline" })
                @Html.EditorFor(model => model.uName)
            </div>
            <div>
                @Html.Label("Password", new { style = "display:inline" })
                @Html.EditorFor(model => model.password)
            </div>
            <input type="submit" class="btn-custom" value="Submit" />
            @*@Html.ActionLink("Submit", "Login", "Donators")*@

        </fieldset>

}
    <p>@ViewBag.U</p>
    <p>@ViewBag.P</p>


